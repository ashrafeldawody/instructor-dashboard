import Vue from "vue";
import Vuex from "vuex";
import { Instructor } from "@/apis/instructor";
Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    Instructor
  }
});
